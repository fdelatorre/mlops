# Modelo de deteccion de cancer por medio de tumores malignos o no

#Librerias

pip install scikit-learn
pip install pandas
pip install numpy
pip install -U imbalanced-learn
pip install pickle
pip install seaborn
pip install -U matplotlib
